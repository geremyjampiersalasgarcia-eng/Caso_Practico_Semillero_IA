# ingest.py - Script de indexación de documentos para ChromaDB
# Carga TXT/PDF (loader), los divide en fragmentos (splitter) y guarda embeddings
# Se ejecuta de forma asíncrona o independiente a la API (offline process)
