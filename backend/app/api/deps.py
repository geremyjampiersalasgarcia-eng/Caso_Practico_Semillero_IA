# deps.py - Dependencias específicas de la capa API REST
# Autenticación JWT (get_current_user), rate limiting (slowapi) y validaciones HTTP
# Estas dependencias se inyectan directamente en los endpoints de los routers
