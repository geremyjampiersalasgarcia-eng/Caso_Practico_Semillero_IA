// api.ts - Cliente Axios o Fetch para comunicación con el Backend
// Contiene las funciones que hacen los llamados HTTP a FastAPI (ej. POST /api/v1/chat)
// Maneja interceptores para agregar tokens, headers o parsear errores genéricos
