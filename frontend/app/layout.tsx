// layout.tsx - Plantilla principal de Next.js (App Router)
// Define el HTML, el head (metadatos) y el body base para toda la aplicación
// Se usa para inyectar fuentes (ej. Inter), contextos globales y layout principal
